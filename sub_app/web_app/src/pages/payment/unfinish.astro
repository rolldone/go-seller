---
export const prerender = false;

import { buildPaymentReturnRedirectPath } from "@/lib/paymentRedirect";

return Astro.redirect(buildPaymentReturnRedirectPath("unfinish", Astro.url.searchParams), 302);
---