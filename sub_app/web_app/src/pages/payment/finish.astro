---
export const prerender = false;

import { buildPaymentReturnRedirectPath } from "@/lib/paymentRedirect";

return Astro.redirect(buildPaymentReturnRedirectPath("finish", Astro.url.searchParams), 302);
---