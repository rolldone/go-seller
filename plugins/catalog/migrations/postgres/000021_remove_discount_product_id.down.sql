-- Migration: 000021_remove_discount_product_id.down.sql
-- Purpose: NO-OP down migration. This project intentionally drops `discounts.product_id` and
-- does not restore it when rolling back. The developer confirmed this decision.

-- Intentionally left empty.
